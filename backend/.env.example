# This file is an example. Copy it to a new file named .env
#  and fill in your actual credentials.

# Spotify API Credentials
SPOTIPY_CLIENT_ID="YOUR_SPOTIFY_CLIENT_ID"
SPOTIPY_CLIENT_SECRET="YOUR_SPOTIFY_CLIENT_SECRET"
# For local development:
SPOTIPY_REDIRECT_URI="http://127.0.0.1:8888/callback"
# For production (update in Spotify Developer Dashboard):
# SPOTIPY_REDIRECT_URI="https://spotify-tracker-api.icg-pi-cluster.click/callback"

# Flask secret key for session management.
#  You can generate a good one by running this in a Python shell:
#  import os; print(os.urandom(24).hex())
FLASK_SECRET_KEY="YOUR_SUPER_SECRET_FLASK_KEY"

# Database Configuration
# For local development with SQLite:
# DATABASE_URL="sqlite:///trackify.db"
# For production with PostgreSQL:
# DATABASE_URL="postgresql://username:password@hostname:5432/database_name"
DATABASE_URL="sqlite:///trackify.db"

# CORS Origins (comma-separated list of allowed origins)
# For local development:
# CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:8888"
# For production with GitHub Pages:
CORS_ORIGINS="https://arthurtolley.github.io,https://spotify-tracker-api.icg-pi-cluster.click"

